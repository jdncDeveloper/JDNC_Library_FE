import { styled } from "styled-components";

const Container = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  max-width: 800px;
  margin: 0 auto;
  grid-gap: 8px;
`;

export default {
  Container,
}
