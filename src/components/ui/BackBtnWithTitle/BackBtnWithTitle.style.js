import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { styled } from "styled-components";

const Container = styled.div`
    width: 100px;
    height: 40px;
`;
const BackIcon = styled(FontAwesomeIcon)`
    font-size: 26px;
`;
export default { Container, BackIcon };
